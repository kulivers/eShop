# Password Manager Feature

Полнофункциональный менеджер паролей с категоризацией, поиском, фильтрацией и генерацией безопасных паролей.

## 🎯 Возможности

- ✅ **CRUD операции** для паролей (создание, чтение, обновление, удаление)
- ✅ **Категоризация** паролей по типам (Соцсети, Email, Банки, Работа, Покупки, Развлечения, Другое)
- ✅ **Поиск** по названию сайта и имени пользователя с debounce
- ✅ **Фильтрация** по категориям
- ✅ **Генератор паролей** с настройкой длины и типов символов
- ✅ **Индикатор силы пароля** в реальном времени
- ✅ **Копирование в буфер обмена** для паролей и имен пользователей
- ✅ **Группировка** по категориям с Accordion
- ✅ **Адаптивный дизайн** (Desktop, Tablet, Mobile)
- ✅ **Toast уведомления** для действий пользователя
- ✅ **Валидация форм** с Formik + Yup
- ✅ **TypeScript** типизация
- ✅ **Redux** для управления состоянием

## 📁 Структура файлов

```
src/features/passwords/
├── components/
│   ├── PasswordManager.tsx        # Главный контейнер
│   ├── PasswordForm.tsx           # Форма добавления/редактирования
│   ├── PasswordCard.tsx           # Карточка аккаунта
│   ├── PasswordList.tsx           # Список/сетка карточек
│   ├── SearchBar.tsx              # Поиск
│   ├── CategoryFilter.tsx         # Фильтр категорий
│   ├── PasswordGenerator.tsx      # Генератор паролей
│   ├── ConfirmDialog.tsx          # Диалог подтверждения
│   └── index.ts                   # Экспорты компонентов
├── utils/
│   ├── passwordStrength.ts        # Проверка силы пароля
│   └── categoryIcons.tsx          # Иконки категорий
├── passwordsSlice.ts              # Redux slice
├── passwordsSelectors.ts          # Redux selectors
├── types.ts                       # TypeScript типы
├── index.ts                       # Главный экспорт
└── README.md                      # Этот файл
```

## 🚀 Использование

### Базовое использование

```tsx
import { PasswordManager } from './features/passwords';

function App() {
  return <PasswordManager />;
}
```

### Использование отдельных компонентов

```tsx
import { 
  PasswordCard, 
  PasswordGenerator,
  SearchBar 
} from './features/passwords';

// Использование карточки пароля
<PasswordCard
  account={account}
  onEdit={handleEdit}
  onDelete={handleDelete}
/>

// Использование генератора паролей
<PasswordGenerator
  onUsePassword={(password) => console.log(password)}
  showUseButton={true}
/>

// Использование поиска
<SearchBar
  value={searchQuery}
  onChange={handleSearchChange}
/>
```

### Использование Redux

```tsx
import { useSelector, useDispatch } from 'react-redux';
import {
  selectFilteredPasswords,
  addPassword,
  updatePassword,
  deletePassword
} from './features/passwords';

function MyComponent() {
  const dispatch = useDispatch();
  const passwords = useSelector(selectFilteredPasswords);
  
  const handleAdd = (data) => {
    dispatch(addPassword({
      id: uuidv4(),
      ...data,
      createdAt: new Date(),
      updatedAt: new Date(),
    }));
  };
  
  return (
    <div>
      {passwords.map(pwd => (
        <div key={pwd.id}>{pwd.website}</div>
      ))}
    </div>
  );
}
```

## 🎨 Компоненты

### PasswordManager

Главный контейнер, объединяющий все функции password manager.

**Props:** Нет (использует Redux)

### PasswordForm

Форма для добавления/редактирования паролей с валидацией.

**Props:**
- `account?: Account` - Аккаунт для редактирования
- `onSubmit: (data: PasswordFormData) => void` - Callback при отправке формы
- `onCancel?: () => void` - Callback при отмене
- `loading?: boolean` - Состояние загрузки

### PasswordCard

Карточка для отображения одного аккаунта.

**Props:**
- `account: Account` - Данные аккаунта
- `onEdit?: (account: Account) => void` - Callback при редактировании
- `onDelete?: (account: Account) => void` - Callback при удалении
- `elevation?: number` - Высота тени карточки (по умолчанию 2)

### PasswordGenerator

Генератор безопасных паролей с настройками.

**Props:**
- `onUsePassword?: (password: string) => void` - Callback при использовании пароля
- `initialPassword?: string` - Начальный пароль
- `showUseButton?: boolean` - Показывать кнопку "Использовать" (по умолчанию true)

### SearchBar

Поле поиска с debounce.

**Props:**
- `value: string` - Текущее значение поиска
- `onChange: (query: string) => void` - Callback при изменении
- `placeholder?: string` - Placeholder текст
- `debounceMs?: number` - Задержка debounce в мс (по умолчанию 300)
- `disabled?: boolean` - Отключен ли поиск

### CategoryFilter

Фильтр по категориям с chips.

**Props:**
- `selectedCategory: string | null` - Выбранная категория
- `categoryCounts: CategoryCount[]` - Количество в каждой категории
- `totalCount: number` - Общее количество
- `onCategorySelect: (category: string | null) => void` - Callback при выборе
- `disabled?: boolean` - Отключен ли фильтр
- `showCounts?: boolean` - Показывать ли количество (по умолчанию true)

### PasswordList

Список паролей с группировкой по категориям.

**Props:**
- `accounts: Account[]` - Массив аккаунтов
- `accountsByCategory?: Record<string, Account[]>` - Аккаунты по категориям
- `groupByCategory?: boolean` - Группировать ли по категориям (по умолчанию false)
- `onEdit?: (account: Account) => void` - Callback при редактировании
- `onDelete?: (account: Account) => void` - Callback при удалении
- `showEmptyState?: boolean` - Показывать пустое состояние (по умолчанию true)
- `emptyMessage?: string` - Сообщение для пустого состояния
- `hasActiveFilters?: boolean` - Есть ли активные фильтры

### ConfirmDialog

Переиспользуемый диалог подтверждения.

**Props:**
- `open: boolean` - Открыт ли диалог
- `title: string` - Заголовок
- `message: string` - Сообщение
- `confirmText?: string` - Текст кнопки подтверждения (по умолчанию "Подтвердить")
- `cancelText?: string` - Текст кнопки отмены (по умолчанию "Отмена")
- `confirmColor?: string` - Цвет кнопки подтверждения (по умолчанию "primary")
- `iconType?: string` - Тип иконки (по умолчанию "warning")
- `onConfirm: () => void` - Callback при подтверждении
- `onCancel: () => void` - Callback при отмене
- `loading?: boolean` - Состояние загрузки
- `maxWidth?: string` - Максимальная ширина (по умолчанию "xs")

## 🔧 Утилиты

### passwordStrength.ts

Функции для оценки надежности пароля:
- `calculatePasswordStrength(password: string): PasswordStrength` - Полная оценка
- `getPasswordStrengthColor(level: StrengthLevel): string` - Цвет по уровню
- `getPasswordStrengthText(level: StrengthLevel): string` - Текст по уровню
- `getPasswordRecommendations(password: string): string[]` - Рекомендации

### categoryIcons.tsx

Функции для работы с иконками категорий:
- `getCategoryIcon(category: Category): React.ComponentType` - Получить иконку
- `getCategoryColor(category: Category): string` - Получить цвет
- `getCategoryBackgroundColor(category: Category): string` - Получить фоновый цвет
- `renderCategoryIcon(category: Category, props?: SvgIconProps): ReactElement` - Отрендерить иконку

## 📊 Redux

### Actions

- `addPassword(account: Account)` - Добавить пароль
- `updatePassword(account: Account)` - Обновить пароль
- `deletePassword(id: string)` - Удалить пароль
- `setSearchQuery(query: string)` - Установить поисковый запрос
- `setSelectedCategory(category: string | null)` - Установить категорию
- `clearFilters()` - Очистить все фильтры
- `setLoading(loading: boolean)` - Установить состояние загрузки
- `setError(error: string | null)` - Установить ошибку

### Selectors

- `selectAllPasswords` - Все пароли
- `selectFilteredPasswords` - Отфильтрованные пароли
- `selectPasswordsByCategory` - Пароли по категориям
- `selectCategoryCounts` - Количество в каждой категории
- `selectTotalAccountsCount` - Общее количество
- `selectSearchQuery` - Текущий поисковый запрос
- `selectSelectedCategory` - Выбранная категория
- `selectHasActiveFilters` - Есть ли активные фильтры
- `selectNoResultsFound` - Нет результатов поиска

## 🎨 Категории

| Категория | Цвет | Иконка |
|-----------|------|--------|
| Соцсети | Синий (#2196F3) | Share |
| Email | Зеленый (#4CAF50) | Email |
| Банки | Оранжевый (#FF9800) | AccountBalance |
| Работа | Фиолетовый (#9C27B0) | Work |
| Покупки | Розовый (#E91E63) | ShoppingCart |
| Развлечения | Красный (#F44336) | Movie |
| Другое | Серый (#9E9E9E) | MoreHoriz |

## 🔐 Безопасность

### Генерация паролей
- Использует `crypto.getRandomValues()` для криптографически безопасной генерации
- Настраиваемая длина (8-64 символа)
- Выбор типов символов (uppercase, lowercase, numbers, symbols)

### Оценка надежности
- Проверка длины пароля
- Проверка разнообразия символов
- Обнаружение паттернов (повторения, последовательности)
- 5 уровней надежности: Weak, Fair, Good, Strong, Very Strong

### Копирование в буфер
- Использует современный Clipboard API
- Fallback для старых браузеров
- Опциональная очистка буфера через заданное время

## 📱 Адаптивность

- **Desktop (>960px)**: Grid с 3 колонками
- **Tablet (600-960px)**: Grid с 2 колонками
- **Mobile (<600px)**: Grid с 1 колонкой, FAB для добавления

## 🎯 Будущие улучшения

- [ ] Backend интеграция
- [ ] Шифрование паролей
- [ ] Экспорт/импорт паролей
- [ ] История изменений
- [ ] 2FA для доступа к менеджеру
- [ ] Безопасный шаринг паролей
- [ ] Проверка на утечки паролей
- [ ] Автозаполнение паролей

## 📄 Лицензия

Часть проекта Password Manager Client
