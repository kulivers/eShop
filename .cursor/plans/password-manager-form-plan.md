# Password Manager Form - План реализации

**Дата создания:** 15 ноября 2024  
**Статус:** В разработке

## Обзор

Создание комплексной формы менеджера паролей с категоризацией, отображением аккаунтов, функциями поиска, фильтрации, генерации паролей и копирования в буфер обмена.

---

## Структура данных

### TypeScript интерфейсы

#### Account (основная модель аккаунта)
```typescript
interface Account {
  id: string;
  website: string;
  username: string;
  password: string;
  category: Category;
  notes?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

#### Category (предопределенные категории)
```typescript
enum Category {
  SOCIAL = 'Соцсети',
  EMAIL = 'Email',
  BANKING = 'Банки',
  WORK = 'Работа',
  SHOPPING = 'Покупки',
  ENTERTAINMENT = 'Развлечения',
  OTHER = 'Другое'
}
```

#### PasswordFormData (данные формы для валидации)
```typescript
interface PasswordFormData {
  website: string;
  username: string;
  password: string;
  category: Category;
  notes?: string;
}
```

---

## Архитектура компонентов

### 1. PasswordManager (главная страница)
**Путь:** `src/features/passwords/components/PasswordManager.tsx`

**Ответственность:**
- Контейнер для всех компонентов
- Управление состоянием поиска и фильтров
- Модальные окна добавления/редактирования

**Интеграция:**
- Использует Redux для состояния
- Рендерит SearchBar, CategoryFilter, PasswordList
- Управляет Dialog для PasswordForm

---

### 2. PasswordForm
**Путь:** `src/features/passwords/components/PasswordForm.tsx`

**Функционал:**
- Форма с Formik + Yup валидацией
- Поля: website, username, password, category, notes
- Кнопка генерации пароля
- Индикатор силы пароля
- Показ/скрытие пароля

**Валидация (Yup):**
- website: required, URL format
- username: required, min 3 chars
- password: required, min 8 chars
- category: required, enum
- notes: optional, max 500 chars

---

### 3. PasswordList/PasswordGrid
**Путь:** `src/features/passwords/components/PasswordList.tsx`

**Функционал:**
- Отображение аккаунтов в виде карточек MUI Card
- Группировка по категориям (Accordion или Tabs)
- Каждая карточка: website, username, кнопки копирования/редактирования/удаления
- Пустое состояние при отсутствии данных

---

### 4. PasswordCard
**Путь:** `src/features/passwords/components/PasswordCard.tsx`

**Функционал:**
- Отображение одного аккаунта
- Кнопки: копировать пароль, редактировать, удалить
- Показ/скрытие пароля
- Иконка категории

---

### 5. SearchBar
**Путь:** `src/features/passwords/components/SearchBar.tsx`

**Функционал:**
- Поле поиска с debounce
- Поиск по website и username
- Интеграция с Redux state

---

### 6. CategoryFilter
**Путь:** `src/features/passwords/components/CategoryFilter.tsx`

**Функционал:**
- Chips или кнопки для фильтрации по категориям
- Показ количества аккаунтов в каждой категории
- "Все" для сброса фильтра

---

### 7. PasswordGenerator
**Путь:** `src/features/passwords/components/PasswordGenerator.tsx`

**Функционал:**
- Настройки генерации:
  - Длина пароля (8-64 символа)
  - Uppercase letters
  - Lowercase letters
  - Numbers
  - Special symbols
- Кнопка генерации
- Индикатор силы пароля
- Копирование сгенерированного пароля

---

### 8. ConfirmDialog
**Путь:** `src/features/passwords/components/ConfirmDialog.tsx`

**Функционал:**
- Переиспользуемый диалог подтверждения
- Используется для удаления аккаунтов
- Настраиваемый текст и действия

---

## Redux архитектура

### Slice: passwordsSlice
**Путь:** `src/features/passwords/passwordsSlice.ts`

#### State структура
```typescript
interface PasswordsState {
  accounts: Account[];
  selectedCategory: string | null;
  searchQuery: string;
  loading: boolean;
  error: string | null;
}
```

#### Actions
- `addPassword(account: Account)` - добавить новый аккаунт
- `updatePassword(account: Account)` - обновить существующий
- `deletePassword(id: string)` - удалить аккаунт
- `setSearchQuery(query: string)` - установить поисковый запрос
- `setSelectedCategory(category: string | null)` - установить фильтр категории

---

### Selectors
**Путь:** `src/features/passwords/passwordsSelectors.ts`

```typescript
- selectAllPasswords - все аккаунты
- selectFilteredPasswords - отфильтрованные по поиску и категории
- selectPasswordsByCategory - группировка по категориям
- selectCategoryCounts - количество в каждой категории
```

---

## Утилиты

### 1. Password Strength
**Путь:** `src/features/passwords/utils/passwordStrength.ts`

**Функции:**
- `calculatePasswordStrength(password: string): StrengthLevel`
- `getPasswordStrengthColor(level: StrengthLevel): string`
- `getPasswordStrengthText(level: StrengthLevel): string`

**Уровни:** Weak, Fair, Good, Strong, Very Strong

---

### 2. Category Icons
**Путь:** `src/features/passwords/utils/categoryIcons.ts`

**Функции:**
- `getCategoryIcon(category: Category): React.ReactElement`
- `getCategoryColor(category: Category): string`

**Иконки (Material-UI):**
- Соцсети: Share, Group
- Email: Email, MailOutline
- Банки: AccountBalance, CreditCard
- Работа: Work, Business
- Покупки: ShoppingCart, Store
- Развлечения: Movie, Videogame
- Другое: MoreHoriz

---

### 3. Clipboard Utils
**Путь:** `src/utils/clipboard.ts`

**Функции:**
- `copyToClipboard(text: string): Promise<boolean>`
- Обработка ошибок
- Показ уведомлений

---

## Основные сценарии использования

### 1. Просмотр всех паролей
**Шаги:**
1. Пользователь открывает страницу PasswordManager
2. Отображаются все аккаунты, сгруппированные по категориям
3. Пароли скрыты по умолчанию (показаны как ••••••)

---

### 2. Добавление нового аккаунта
**Шаги:**
1. Клик на кнопку "Добавить пароль"
2. Открывается Modal с PasswordForm
3. Заполнение всех полей
4. (Опционально) Генерация пароля через PasswordGenerator
5. Валидация формы
6. Сохранение → добавление в Redux store
7. Toast уведомление "Аккаунт добавлен"
8. Закрытие модального окна

---

### 3. Редактирование аккаунта
**Шаги:**
1. Клик на кнопку "Редактировать" на карточке
2. Открывается Modal с заполненной формой
3. Изменение данных
4. Валидация
5. Сохранение → обновление в Redux store
6. Toast уведомление "Аккаунт обновлен"
7. Закрытие модального окна

---

### 4. Удаление аккаунта
**Шаги:**
1. Клик на кнопку "Удалить" на карточке
2. Открывается ConfirmDialog с предупреждением
3. Подтверждение удаления
4. Удаление из Redux store
5. Toast уведомление "Аккаунт удален"
6. Закрытие диалога

---

### 5. Поиск аккаунтов
**Шаги:**
1. Ввод текста в SearchBar
2. Debounce 300ms
3. Dispatch setSearchQuery action
4. Selector фильтрует accounts по website/username
5. Отображение отфильтрованных результатов
6. Если нет результатов → Empty State

---

### 6. Фильтрация по категориям
**Шаги:**
1. Клик на chip категории
2. Dispatch setSelectedCategory action
3. Selector фильтрует accounts по категории
4. Отображение только выбранной категории
5. Клик на "Все" → сброс фильтра

---

### 7. Копирование пароля
**Шаги:**
1. Клик на кнопку "Копировать" (ContentCopy icon)
2. Вызов copyToClipboard(password)
3. Пароль копируется в буфер обмена
4. Toast уведомление "Пароль скопирован"
5. (Опционально) Временный показ пароля на 2 секунды

---

### 8. Генерация безопасных паролей
**Шаги:**
1. В PasswordForm клик "Сгенерировать пароль"
2. Открывается Drawer/Dialog с PasswordGenerator
3. Настройка параметров:
   - Длина
   - Типы символов (uppercase, lowercase, numbers, symbols)
4. Клик "Сгенерировать"
5. Отображение пароля с индикатором силы
6. Клик "Использовать" → пароль вставляется в форму
7. Или "Копировать" → пароль в буфер обмена

---

## Файловая структура

```
src/
├── features/
│   └── passwords/
│       ├── components/
│       │   ├── PasswordManager.tsx        # Главный контейнер
│       │   ├── PasswordForm.tsx           # Форма добавления/редактирования
│       │   ├── PasswordCard.tsx           # Карточка аккаунта
│       │   ├── PasswordList.tsx           # Список/сетка карточек
│       │   ├── SearchBar.tsx              # Поиск
│       │   ├── CategoryFilter.tsx         # Фильтр категорий
│       │   ├── PasswordGenerator.tsx      # Генератор паролей
│       │   └── ConfirmDialog.tsx          # Диалог подтверждения
│       ├── passwordsSlice.ts              # Redux slice
│       ├── passwordsSelectors.ts          # Redux selectors
│       ├── types.ts                       # TypeScript типы
│       └── utils/
│           ├── passwordStrength.ts        # Проверка силы пароля
│           └── categoryIcons.ts           # Иконки категорий
├── utils/
│   └── clipboard.ts                       # Копирование в буфер
└── redux/
    └── store.ts                           # Обновить для включения passwordsSlice
```

---

## UI/UX спецификации

### Material-UI компоненты
- **Card**: для карточек аккаунтов
- **TextField**: для полей ввода
- **Button, IconButton**: для действий
- **Chip**: для категорий
- **Dialog**: для модальных окон
- **Drawer**: для генератора паролей
- **Snackbar** (notistack): для уведомлений
- **Grid**: для адаптивной верстки
- **Accordion/Tabs**: для группировки категорий

### Иконки (Material-UI Icons)
- **Visibility/VisibilityOff**: показ/скрытие пароля
- **ContentCopy**: копирование
- **Edit**: редактирование
- **Delete**: удаление
- **Add**: добавление нового
- **Search**: поиск
- **Refresh**: регенерация пароля

### Цветовая схема категорий
- Соцсети: blue (#2196F3)
- Email: green (#4CAF50)
- Банки: orange (#FF9800)
- Работа: purple (#9C27B0)
- Покупки: pink (#E91E63)
- Развлечения: red (#F44336)
- Другое: grey (#9E9E9E)

### Адаптивность
- **Desktop (>960px)**: Grid 3 колонки
- **Tablet (600-960px)**: Grid 2 колонки
- **Mobile (<600px)**: Grid 1 колонка

### Темная тема
- Поддержка через MUI ThemeProvider
- Автоматическая адаптация всех компонентов

---

## Валидация формы (Yup)

```typescript
const validationSchema = yup.object({
  website: yup
    .string()
    .required('Обязательное поле')
    .url('Введите корректный URL'),
  username: yup
    .string()
    .required('Обязательное поле')
    .min(3, 'Минимум 3 символа'),
  password: yup
    .string()
    .required('Обязательное поле')
    .min(8, 'Минимум 8 символов'),
  category: yup
    .string()
    .required('Выберите категорию')
    .oneOf(Object.values(Category)),
  notes: yup
    .string()
    .max(500, 'Максимум 500 символов')
});
```

---

## Безопасность

### Хранение паролей (пока только UI)
- Пароли хранятся в Redux state (в памяти)
- **НЕ** хранить в localStorage без шифрования
- В будущем: интеграция с бэкендом и шифрование

### Генерация паролей
- Использование crypto.getRandomValues() для безопасной генерации
- Минимум 8 символов
- Рекомендация: 16+ символов с разными типами

### Копирование в буфер
- Использование Clipboard API
- Fallback для старых браузеров
- Очистка чувствительных данных

---

## Задачи (To-Do List)

### ✅ = Выполнено | 🔄 = В процессе | ⏳ = Ожидает

1. ✅ Создать TypeScript типы и интерфейсы
2. ✅ Создать Redux slice с actions и reducers
3. ✅ Создать селекторы для фильтрации и поиска
4. ✅ Создать утилиты (passwordStrength, categoryIcons, clipboard)
5. ✅ Создать компонент PasswordGenerator
6. ✅ Создать PasswordForm с Formik + Yup
7. ✅ Создать ConfirmDialog
8. ✅ Создать PasswordCard
9. ✅ Создать SearchBar и CategoryFilter
10. ✅ Создать PasswordList с группировкой
11. ✅ Создать главный компонент PasswordManager
12. ✅ Интегрировать passwordsSlice в store
13. ✅ Добавить toast уведомления (notistack)

---

## Тестирование (будущее)

### Unit тесты
- Redux reducers и actions
- Селекторы
- Утилиты (passwordStrength, clipboard)
- Валидация схемы

### Integration тесты
- Взаимодействие компонентов
- Redux flow
- Формы с валидацией

### E2E тесты
- Полные пользовательские сценарии
- CRUD операции
- Поиск и фильтрация

---

## Следующие этапы (после UI)

1. **Backend интеграция**
   - API endpoints для CRUD
   - Аутентификация
   - Шифрование паролей

2. **Улучшения**
   - Экспорт/импорт паролей
   - Теги и множественные категории
   - История изменений
   - Шаринг паролей (безопасный)
   - 2FA для доступа к менеджеру

3. **Производительность**
   - Виртуализация списков (react-window)
   - Lazy loading компонентов
   - Мемоизация селекторов (Reselect)

---

**Конец документа**

